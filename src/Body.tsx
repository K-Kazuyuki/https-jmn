import { Typography, Container } from "@mui/material";
import "swiper/swiper-bundle.css";
import "swiper/swiper.min.css";

const Body = () => {
  return (
    <Container>
      <Typography variant="h5">特に，なにも，出すものがない</Typography>
    </Container>
  );
};

export default Body;
